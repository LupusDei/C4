// DesignKit — Synthesis Design System for C4
// Provides theme, components, and modifiers for the warm editorial aesthetic.

import SwiftUI

/// Re-export all public types for convenient `import DesignKit` usage.
public enum DesignKit {
    public static let version = "1.0.0"
}
